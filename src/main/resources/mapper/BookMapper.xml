<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.newtest.mapper.BookMapper">

    <resultMap id="BaseResultMap" type="com.example.newtest.enity.Book">
            <id property="id" column="id" jdbcType="INTEGER"/>
            <result property="name" column="name" jdbcType="VARCHAR"/>
            <result property="author" column="author" jdbcType="VARCHAR"/>
            <result property="publish" column="publish" jdbcType="VARCHAR"/>
            <result property="pages" column="pages" jdbcType="INTEGER"/>
            <result property="price" column="price" jdbcType="FLOAT"/>
            <result property="bookcaseid" column="bookcaseid" jdbcType="INTEGER"/>
            <result property="abled" column="abled" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,name,author,
        publish,pages,price,
        bookcaseid,abled
    </sql>
    <select id="findByName" resultType="com.example.newtest.enity.Book">
        select * from my_book where
                               1=1
--                                 此处需要 == 
            <if test="name != null and name != '' and name == '1'">
                and
                name in (${name})
            </if>

    </select>
    <select id="selectName" resultType="com.example.newtest.enity.Book">
        select #{name},#{name1} from my_book;
    </select>
</mapper>
